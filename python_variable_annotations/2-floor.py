#!/usr/bin/env python3
import math
"""create a floor function that returns it from a number"""


def floor(n: float) -> int:
    """
    returns the floor of the float variable.

    Args:
    n (float): The number.

    Returns:
    int: the floor of the variable.
    """
    return math.floor(n)
